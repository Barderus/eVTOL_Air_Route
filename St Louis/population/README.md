# St. Louis Population Data

Place the selected source population data in this folder. Update
`POPULATION_SOURCE` in `St Louis/settings.py`, then implement the source-specific
preprocessing in `St Louis/build_population.py`.

The processed population-density GeoJSON is generated and should not be
committed.
